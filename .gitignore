# ==========================================
# 1. 安全性 (Security) - 絕對不能上傳
# ==========================================
.env
.env.test
.env.production
# 防止 Google Session 或 Puppeteer Cookies 洩漏
google-session.json
*.cookie
*.json.last

# ==========================================
# 2. Node.js 核心
# ==========================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
# ^ 建議保留 package-lock.json 以鎖定依賴版本，若不習慣可註解掉

# ==========================================
# 3. Golem V9.0 記憶體與資料庫 (Memory & DB)
# ==========================================
# Titan Chronos 的排程與隊列數據
queue/
*.db
*.sqlite
*.sqlite-journal

# 向量記憶體與知識庫 (Local RAG)
golem_memory/
memory/knowledge/
# 如果您希望保留預設記憶模板，可使用 !golem_memory/template/

# Transformers.js 模型緩存 (避免上傳幾百 MB 的模型檔)
.cache/
models/
ONNX/

# ==========================================
# 4. Puppeteer 瀏覽器暫存 (Browser Data)
# ==========================================
# Puppeteer 下載的 Chromium (如果裝在專案內)
.local-chromium/
.puppeteer/

# 使用者資料夾 (含登入 Session，檔案巨大且敏感)
user_data/
profile/
puppeteer_data/

# ==========================================
# 5. 執行日誌與除錯 (Logs & Debug)
# ==========================================
logs/
*.log
error.txt
test_output*.txt
combined.log
coverage/
.nyc_output/

# 截圖與錄影 (Agent 執行失敗時的截圖)
screenshots/
videos/
*.mp4

# ==========================================
# 6. 系統雜檔 (System Junk)
# ==========================================
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
temp/
tmp/
backups/
golems.json
moltbot_auth.json
.golem.pid
